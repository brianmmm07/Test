version: '3.8'

services:
  arxiv-summarizer:
    build: .
    container_name: arxiv-summarizer
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - TZ=UTC
